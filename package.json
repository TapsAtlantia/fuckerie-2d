{
  "name": "fuckerie-2d",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Hybrid 2D sandbox engine (Terraria core + Mindustry/Minecraft/Stardew) — Phase 1 prototype",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "build:artifact": "node scripts/make-artifact.mjs artifact/index.html",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "peerjs": "^1.5.5"
  }
}
